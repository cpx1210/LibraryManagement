<h1>创建用户与目录</h1>

创建专用用户：sudo useradd -r -s /sbin/nologin minio-user

*# 创建数据目录*

sudo mkdir -p /data/minio

sudo chown -R minio-user:minio-user /data/minio

*# 创建配置目录*

sudo mkdir -p /etc/minio

sudo chown minio-user:minio-user /etc/minio



创建