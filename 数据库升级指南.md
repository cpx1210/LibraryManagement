# 数据库升级指南 v1.0.0 → v1.1.0

## 📋 升级概述

本指南说明如何将现有的数据库从 v1.0.0 升级到 v1.1.0，**不会删除任何现有数据**。

**升级日期**: 2025-10-30
**目标版本**: v1.1.0

---

## 🎯 升级内容

### 1. sensitive_words 表
- ✅ 新增 `detection_type` 字段（检测类型）
- ✅ 新增 `alert_message` 字段（警报信息）
- ✅ 新增索引 `idx_sw_detection_type`

### 2. booklist_check_detail 表
- ✅ 新增 10 个字段以支持新的书目模板：
  - `book_number` - 书号
  - `subtitle` - 副题名
  - `author1` - 著者1
  - `author2` - 著者2
  - `publish_location` - 出版地
  - `publish_date` - 出版日期
  - `target_audience` - 读者对象
  - `content_summary` - 内容简介
  - `classification_number` - 分类号
  - `language` - 作品语种

---

## 🚀 快速升级（一键执行）

### 方法一：使用 psql 命令行（推荐）

```bash
# 1. 备份数据库（重要！）
pg_dump -U postgres library_management > backup_$(date +%Y%m%d_%H%M%S).sql

# 2. 执行迁移脚本
cd library-management-backend/src/main/resources/db
psql -U postgres -d library_management -f migrate_to_v1.1.0.sql
```

### 方法二：使用图形化工具

1. 打开 DBeaver / pgAdmin / Navicat
2. 连接到 `library_management` 数据库
3. 打开 SQL 编辑器
4. 粘贴 `migrate_to_v1.1.0.sql` 的全部内容
5. 执行脚本

---

## 📝 详细步骤

### 步骤 1: 备份数据库（必须！）

```bash
# 创建备份文件
pg_dump -U postgres library_management > backup_before_upgrade.sql

# 验证备份文件
ls -lh backup_before_upgrade.sql

# 可选：压缩备份
gzip backup_before_upgrade.sql
```

**⚠️ 重要**: 在任何数据库变更前，**必须**先备份！

---

### 步骤 2: 执行迁移脚本

```bash
# 连接到数据库并执行
psql -U postgres -d library_management -f migrate_to_v1.1.0.sql
```

**预期输出**：
```
开始更新 sensitive_words 表...
✓ 已添加 detection_type 字段
✓ 已添加 alert_message 字段
✓ 已创建 idx_sw_detection_type 索引
✅ sensitive_words 表更新完成

开始更新 booklist_check_detail 表...
✓ 已添加 book_number 字段
✓ 已添加 subtitle 字段
✓ 已添加 author1 字段并迁移数据
✓ 已添加 author2 字段
✓ 已添加 publish_location 字段
✓ 已添加 publish_date 字段
✓ 已添加 target_audience 字段
✓ 已添加 content_summary 字段
✓ 已添加 classification_number 字段
✓ 已添加 language 字段
✅ booklist_check_detail 表更新完成

========================================
开始验证迁移结果...
========================================

【sensitive_words 表】
  ✓ detection_type 字段存在
  ✓ alert_message 字段存在

【booklist_check_detail 表】
  ✓ book_number 字段存在
  ✓ subtitle 字段存在
  ✓ author1 字段存在
  ✓ author2 字段存在
  ✓ publish_location 字段存在
  ✓ publish_date 字段存在
  ✓ target_audience 字段存在
  ✓ content_summary 字段存在
  ✓ classification_number 字段存在
  ✓ language 字段存在

========================================
✅ 所有字段验证通过！
数据库已成功升级到 v1.1.0
========================================

COMMIT
```

---

### 步骤 3: 验证升级结果

```sql
-- 连接到数据库
psql -U postgres -d library_management

-- 1. 验证 sensitive_words 表的新字段
SELECT column_name, data_type, character_maximum_length
FROM information_schema.columns
WHERE table_name = 'sensitive_words'
  AND column_name IN ('detection_type', 'alert_message')
ORDER BY column_name;

-- 期望输出：
--  column_name    | data_type         | character_maximum_length
-- ----------------+-------------------+-------------------------
--  alert_message  | character varying | 200
--  detection_type | character varying | 20

-- 2. 验证 booklist_check_detail 表的新字段
SELECT column_name, data_type
FROM information_schema.columns
WHERE table_name = 'booklist_check_detail'
  AND column_name IN (
    'book_number', 'subtitle', 'author1', 'author2',
    'publish_location', 'publish_date', 'target_audience',
    'content_summary', 'classification_number', 'language'
  )
ORDER BY column_name;

-- 期望输出：应该看到 10 个字段

-- 3. 检查索引
SELECT indexname
FROM pg_indexes
WHERE tablename = 'sensitive_words'
  AND indexname = 'idx_sw_detection_type';

-- 期望输出：idx_sw_detection_type

-- 4. 验证数据完整性
SELECT COUNT(*) FROM sensitive_words;
SELECT COUNT(*) FROM booklist_check_detail;

-- 5. 检查现有数据是否保留
SELECT * FROM sys_user LIMIT 5;

-- 退出
\q
```

---

## ✅ 升级特点

### 1. 安全性
- ✅ 使用事务（BEGIN/COMMIT）
- ✅ 可重复执行（幂等性）
- ✅ 自动验证
- ✅ 数据零丢失

### 2. 智能检查
- ✅ 字段存在性检查（IF NOT EXISTS）
- ✅ 索引存在性检查
- ✅ 自动跳过已存在的字段
- ✅ 友好的进度提示

### 3. 数据迁移
- ✅ 自动迁移 `author` → `author1`
- ✅ 保留所有现有数据
- ✅ 新字段允许 NULL（兼容旧数据）

---

## 🔄 重复执行

**脚本可以安全地重复执行**，如果字段已存在，会自动跳过：

```bash
# 第一次执行
psql -U postgres -d library_management -f migrate_to_v1.1.0.sql
# 输出：✓ 已添加 xxx 字段

# 第二次执行（如果需要）
psql -U postgres -d library_management -f migrate_to_v1.1.0.sql
# 输出：○ xxx 字段已存在，跳过
```

---

## 🛡️ 回滚方案

如果升级后发现问题，可以回滚到备份：

### 方法一：从备份恢复（推荐）

```bash
# 1. 删除当前数据库
psql -U postgres -c "DROP DATABASE library_management;"

# 2. 创建新数据库
psql -U postgres -c "CREATE DATABASE library_management WITH ENCODING='UTF8';"

# 3. 恢复备份
psql -U postgres -d library_management < backup_before_upgrade.sql

# 或者恢复压缩备份
gunzip -c backup_before_upgrade.sql.gz | psql -U postgres -d library_management
```

### 方法二：手动删除新字段

```sql
-- 连接到数据库
psql -U postgres -d library_management

-- 删除 sensitive_words 表的新字段
ALTER TABLE sensitive_words DROP COLUMN IF EXISTS detection_type;
ALTER TABLE sensitive_words DROP COLUMN IF EXISTS alert_message;
DROP INDEX IF EXISTS idx_sw_detection_type;

-- 删除 booklist_check_detail 表的新字段
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS book_number;
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS subtitle;
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS author1;
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS author2;
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS publish_location;
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS publish_date;
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS target_audience;
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS content_summary;
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS classification_number;
ALTER TABLE booklist_check_detail DROP COLUMN IF EXISTS language;

COMMIT;
```

---

## ❓ 常见问题

### Q1: 执行脚本时提示权限错误

**解决方案**: 使用超级用户执行：
```bash
psql -U postgres -d library_management -f migrate_to_v1.1.0.sql
```

### Q2: 脚本执行中断怎么办？

**解决方案**: 脚本使用了事务，如果中断，所有更改会自动回滚。重新执行即可：
```bash
psql -U postgres -d library_management -f migrate_to_v1.1.0.sql
```

### Q3: 如何验证升级是否成功？

**解决方案**: 脚本内置验证功能，执行完成后会自动显示验证结果。也可以手动验证：
```sql
SELECT column_name FROM information_schema.columns
WHERE table_name = 'sensitive_words'
  AND column_name IN ('detection_type', 'alert_message');
```

### Q4: 升级会影响现有数据吗？

**解决方案**: 不会！脚本只添加新字段，不修改或删除现有数据。唯一的数据迁移是将 `author` 字段的值复制到 `author1`。

### Q5: 升级需要停止应用吗？

**建议**:
- 开发/测试环境：不需要
- 生产环境：建议在维护窗口执行，停止应用5-10分钟

### Q6: 升级需要多长时间？

**时间估算**:
- 数据量 < 1万：< 1秒
- 数据量 1万-10万：1-5秒
- 数据量 > 10万：5-30秒

---

## 📊 升级前后对比

### 升级前（v1.0.0）
- sensitive_words: 10 个字段
- booklist_check_detail: 16 个字段

### 升级后（v1.1.0）
- sensitive_words: **12 个字段** (+2)
- booklist_check_detail: **26 个字段** (+10)

---

## 📖 相关文档

- [migrate_to_v1.1.0.sql](library-management-backend/src/main/resources/db/migrate_to_v1.1.0.sql) - 完整迁移脚本
- [新模板适配说明.md](新模板适配说明.md) - 新功能说明
- [数据库脚本清理说明.md](数据库脚本清理说明.md) - 脚本整理记录

---

## ✨ 升级清单

升级前请确认：

- [ ] 已阅读本文档
- [ ] 已备份数据库
- [ ] 已在测试环境验证
- [ ] 已通知相关人员
- [ ] 已准备回滚方案

升级后请确认：

- [ ] 脚本执行成功
- [ ] 所有验证通过
- [ ] 数据完整性检查通过
- [ ] 应用程序正常运行
- [ ] 新功能可用

---

## 📞 技术支持

如遇问题：
1. 查看脚本输出的错误信息
2. 检查 PostgreSQL 日志
3. 参考本文档"常见问题"部分
4. 联系技术支持团队

---

**升级脚本准备完成！** 🎉

现在可以安全地升级您的数据库到 v1.1.0 版本。
