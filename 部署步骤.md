# 🚀 图书馆管理系统部署步骤（内网环境）

## ✅ 当前环境

- 服务器：Ubuntu 22.04
- Docker版本：28.2.2
- docker-compose版本：1.29.2
- 网络：内网环境（无外网访问）
- 已配置内网Docker镜像源

---

## 📦 第一步：上传项目文件到服务器

### 需要上传的文件和文件夹

```
从本地上传以下内容到服务器 /root/library-management/ 目录：

✅ library-management-backend/     # 整个后端文件夹
   ├── src/                        # 源代码
   ├── pom.xml                     # Maven配置
   ├── Dockerfile                  # Docker构建文件
   └── .dockerignore               # Docker忽略文件

✅ library-management-frontend/    # 整个前端文件夹
   ├── src/                        # 源代码
   ├── public/                     # 静态资源
   ├── package.json                # npm依赖配置
   ├── package-lock.json           # npm锁定版本
   ├── vite.config.js              # Vite配置
   ├── index.html                  # 入口HTML
   ├── Dockerfile                  # Docker构建文件
   ├── nginx.conf                  # Nginx配置
   └── .dockerignore               # Docker忽略文件

✅ docker-compose.yml              # Docker编排文件
✅ .env                            # 环境变量配置

❌ 不需要上传（排除）：
   - node_modules/                 # npm依赖（会在构建时下载）
   - target/                       # Maven构建产物
   - dist/                         # 前端构建产物
   - .git/                         # Git仓库
   - .idea/                        # IDE配置
```

### 上传方式

使用你的文件传输工具，将上述文件上传到：
```
服务器路径：/root/library-management/
```

---

## 🔧 第二步：在服务器上检查文件

连接到服务器后，执行：

```bash
cd /root/library-management
ls -la
```

**应该看到：**
```
library-management-backend/
library-management-frontend/
docker-compose.yml
.env
```

### 验证文件完整性

```bash
# 检查后端文件
ls library-management-backend/

# 检查前端文件
ls library-management-frontend/

# 查看环境变量配置
cat .env
```

---

## 🚀 第三步：构建Docker镜像

**⚠️ 注意：** 构建过程需要下载依赖，可能需要10-20分钟，请耐心等待。

### 构建所有服务

```bash
cd /root/library-management
sudo docker-compose build
```

**构建过程说明：**
- 会先构建后端镜像（下载Maven依赖，编译Java代码）
- 然后构建前端镜像（下载npm依赖，编译Vue代码）
- 会从内网镜像源拉取MySQL、Node、Maven、Nginx等基础镜像

### 查看构建进度

构建时会显示详细的日志，包括：
- Downloading artifacts...（下载依赖）
- Building...（编译代码）
- Successfully built（构建成功）

### 验证镜像构建成功

```bash
sudo docker images | grep library
```

**应该看到：**
```
library-management-backend    latest
library-management-frontend   latest
```

---

## 🎯 第四步：启动所有服务

### 启动服务

```bash
sudo docker-compose up -d
```

**参数说明：**
- `-d`：后台运行（detached mode）

### 查看服务状态

```bash
sudo docker-compose ps
```

**应该看到3个服务都是 UP 状态：**
```
NAME                STATE
library-mysql       Up (healthy)
library-backend     Up (healthy)
library-frontend    Up
```

### 查看服务日志

```bash
# 查看所有服务日志
sudo docker-compose logs -f

# 只查看后端日志
sudo docker-compose logs -f backend

# 只查看前端日志
sudo docker-compose logs -f frontend

# 只查看数据库日志
sudo docker-compose logs -f mysql
```

**按 Ctrl+C 退出日志查看**

---

## ✅ 第五步：验证部署

### 1. 检查容器运行状态

```bash
sudo docker ps
```

所有容器应该都是 Up 状态

### 2. 检查端口监听

```bash
sudo netstat -tlnp | grep -E ':(80|8080)'
```

**应该看到：**
- 80端口：frontend容器监听
- 8080端口：backend容器监听

### 3. 测试后端健康检查

```bash
curl http://localhost:8080/api/actuator/health
```

**应该返回：**
```json
{"status":"UP"}
```

### 4. 测试前端访问

在浏览器中访问：
```
http://服务器IP
```

**应该能看到登录界面**

### 5. 测试API文档

在浏览器中访问：
```
http://服务器IP:8080/api/doc.html
```

**应该能看到API文档界面**

---

## 🎉 第六步：登录测试

### 默认管理员账号

```
用户名：admin
密码：admin123
```

### 测试流程

1. 访问前端：`http://服务器IP`
2. 输入用户名和密码
3. 登录成功后应该能看到系统主界面
4. 测试各个功能模块

---

## 📋 常用管理命令

### 服务管理

```bash
# 停止所有服务
sudo docker-compose stop

# 启动所有服务
sudo docker-compose start

# 重启所有服务
sudo docker-compose restart

# 停止并删除所有容器（数据不会丢失）
sudo docker-compose down

# 重新构建并启动
sudo docker-compose up -d --build
```

### 日志查看

```bash
# 实时查看所有日志
sudo docker-compose logs -f

# 查看最近100行日志
sudo docker-compose logs --tail=100

# 查看特定服务日志
sudo docker-compose logs -f backend
```

### 进入容器

```bash
# 进入后端容器
sudo docker exec -it library-backend bash

# 进入MySQL容器
sudo docker exec -it library-mysql bash

# 在MySQL容器中连接数据库
sudo docker exec -it library-mysql mysql -uroot -p
```

### 数据备份

```bash
# 备份MySQL数据库
sudo docker exec library-mysql mysqldump -uroot -p${MYSQL_ROOT_PASSWORD} library_management > backup_$(date +%Y%m%d).sql

# 恢复数据库
cat backup_20240101.sql | sudo docker exec -i library-mysql mysql -uroot -p${MYSQL_ROOT_PASSWORD} library_management
```

---

## 🐛 常见问题处理

### Q1: 构建失败，提示无法下载依赖

**检查：**
```bash
# 测试能否访问内网镜像源
curl -I http://192.168.10.76:8082
```

**解决：** 确认内网镜像源可用，重新构建

### Q2: 服务启动失败

**检查日志：**
```bash
sudo docker-compose logs backend
```

**常见原因：**
- 数据库未启动完成（等待health check通过）
- 环境变量配置错误
- 端口被占用

### Q3: 前端能访问但后端API失败

**检查：**
```bash
# 测试后端是否正常
curl http://localhost:8080/api/actuator/health

# 检查容器网络
sudo docker network inspect library-management_library-network
```

### Q4: 数据库连接失败

**检查：**
```bash
# 查看MySQL日志
sudo docker-compose logs mysql

# 进入后端容器测试连接
sudo docker exec -it library-backend bash
# 在容器内执行
ping mysql
```

---

## 🔒 安全建议

1. **修改默认密码**
   - 修改 `.env` 中的数据库密码
   - 修改 JWT 密钥
   - 登录后修改管理员密码

2. **数据备份**
   - 定期备份数据库
   - 备份 `.env` 文件

3. **监控日志**
   - 定期查看应用日志
   - 关注异常访问

---

## 📞 需要帮助？

如果遇到问题，请提供：
1. 错误日志：`sudo docker-compose logs`
2. 容器状态：`sudo docker ps -a`
3. 具体错误信息

---

**祝部署顺利！** 🎉

