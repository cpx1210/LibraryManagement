# 📦 图书馆管理系统 - 离线部署指南

## 🎯 部署场景

- **服务器环境**: Ubuntu 22.04
- **网络条件**: 纯内网环境，无法访问外网
- **已有环境**: MySQL数据库、Docker环境
- **部署方式**: 本地打包镜像 → 上传服务器 → 导入运行

---

## 📋 前置准备

### 本地电脑要求（Windows）

- ✅ Windows 10/11 Pro
- ✅ 至少 8GB 内存
- ✅ 至少 20GB 可用磁盘空间
- ✅ 已安装 Docker Desktop
- ✅ 有互联网连接

### 服务器要求（Ubuntu 22.04）

- ✅ 已安装 Docker（版本 20.10+）
- ✅ 已安装 Docker Compose（版本 1.29+）
- ✅ 已安装并运行 MySQL 8.0+
- ✅ 已创建数据库 `library_management`
- ✅ 至少 4GB 可用磁盘空间

---

## 第一部分：本地打包镜像（Windows）

### 步骤 1：安装 Docker Desktop（如未安装）

1. 下载 Docker Desktop
   - 官网：https://www.docker.com/products/docker-desktop/
   - 直接下载：https://desktop.docker.com/win/main/amd64/Docker%20Desktop%20Installer.exe

2. 安装并重启电脑

3. 启动 Docker Desktop，等待就绪（任务栏图标变绿）

4. 验证安装

```powershell
docker --version
docker-compose --version
```

### 步骤 2：执行打包脚本

1. 打开 PowerShell（以管理员身份运行）

2. 进入项目目录

```powershell
cd D:\LibraryManagement
```

3. 执行打包脚本

```powershell
.\build-images.bat
```

**脚本会自动完成以下操作：**
- ✅ 检查 Docker 环境
- ✅ 构建后端镜像（10-15分钟）
- ✅ 构建前端镜像（5-10分钟）
- ✅ 导出为 tar 文件
- ✅ 显示文件大小

**预期输出文件：**
```
library-backend-v1.0.tar    (约 1.5 GB)
library-frontend-v1.0.tar   (约 150 MB)
```

### 步骤 3：准备上传文件

需要上传到服务器的文件：

```
D:\LibraryManagement\
├── library-backend-v1.0.tar        # 后端镜像
├── library-frontend-v1.0.tar       # 前端镜像
├── docker-compose-offline.yml      # Docker配置文件
└── deploy-server.sh                # 服务器部署脚本
```

---

## 第二部分：服务器部署（Ubuntu 22.04）

### 步骤 1：上传文件到服务器

使用 SCP、SFTP 或其他文件传输工具，将上述 4 个文件上传到服务器：

```bash
# 目标路径（建议）
/root/library-management/
```

### 步骤 2：准备 MySQL 数据库

1. 登录 MySQL

```bash
mysql -u root -p
```

2. 创建数据库和用户

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS library_management 
  DEFAULT CHARACTER SET utf8mb4 
  COLLATE utf8mb4_unicode_ci;

-- 创建用户
CREATE USER IF NOT EXISTS 'library_user'@'%' 
  IDENTIFIED BY 'library_pass';

-- 授权
GRANT ALL PRIVILEGES ON library_management.* 
  TO 'library_user'@'%';

FLUSH PRIVILEGES;

-- 退出
EXIT;
```

3. 导入初始数据（如有）

```bash
# 如果有SQL初始化脚本
mysql -u library_user -p library_management < init_data.sql
```

### 步骤 3：修改配置（可选）

如果 MySQL 配置与默认不同，请修改 `deploy-server.sh`：

```bash
nano deploy-server.sh

# 修改这些参数：
MYSQL_HOST="localhost"        # MySQL主机
MYSQL_PORT="3306"             # MySQL端口
MYSQL_DATABASE="library_management"  # 数据库名
MYSQL_USER="library_user"     # 数据库用户名
MYSQL_PASSWORD="library_pass" # 数据库密码
```

也可以修改 `docker-compose-offline.yml` 中的环境变量：

```bash
nano docker-compose-offline.yml

# 修改 backend 服务的环境变量：
DB_URL: jdbc:mysql://localhost:3306/library_management?...
DB_USERNAME: library_user
DB_PASSWORD: library_pass
```

### 步骤 4：执行部署脚本

1. 进入上传目录

```bash
cd /root/library-management
```

2. 添加执行权限

```bash
chmod +x deploy-server.sh
```

3. 执行部署脚本

```bash
sudo ./deploy-server.sh
```

**脚本会自动完成：**
- ✅ 检查系统环境
- ✅ 验证文件完整性
- ✅ 导入 Docker 镜像
- ✅ 创建必要目录
- ✅ 配置环境变量
- ✅ 启动服务容器

### 步骤 5：验证部署

1. 检查容器状态

```bash
docker-compose -f docker-compose-offline.yml ps
```

期望输出：

```
NAME                STATUS              PORTS
library-backend     Up 1 minute         
library-frontend    Up 1 minute         0.0.0.0:80->80/tcp
```

2. 查看日志

```bash
# 查看所有日志
docker-compose -f docker-compose-offline.yml logs

# 只看后端日志
docker-compose -f docker-compose-offline.yml logs -f backend

# 只看前端日志
docker-compose -f docker-compose-offline.yml logs -f frontend
```

3. 测试访问

```bash
# 测试后端API
curl http://localhost:8080/api/actuator/health

# 测试前端
curl http://localhost
```

### 步骤 6：配置防火墙

开放必要端口：

```bash
# Ubuntu UFW 防火墙
sudo ufw allow 80/tcp
sudo ufw allow 8080/tcp

# 或使用 iptables
sudo iptables -A INPUT -p tcp --dport 80 -j ACCEPT
sudo iptables -A INPUT -p tcp --dport 8080 -j ACCEPT
sudo iptables-save > /etc/iptables/rules.v4
```

---

## 🌐 访问系统

部署成功后，可通过以下地址访问：

- **前端界面**: `http://服务器IP地址`
- **后端API**: `http://服务器IP地址:8080/api`

默认登录账号（请查看数据库初始化脚本）：
- 管理员账号：admin / admin123

---

## 🔧 常用运维命令

### 查看服务状态

```bash
docker-compose -f docker-compose-offline.yml ps
```

### 查看实时日志

```bash
docker-compose -f docker-compose-offline.yml logs -f
```

### 重启服务

```bash
# 重启所有服务
docker-compose -f docker-compose-offline.yml restart

# 只重启后端
docker-compose -f docker-compose-offline.yml restart backend

# 只重启前端
docker-compose -f docker-compose-offline.yml restart frontend
```

### 停止服务

```bash
docker-compose -f docker-compose-offline.yml stop
```

### 启动服务

```bash
docker-compose -f docker-compose-offline.yml start
```

### 完全删除服务

```bash
# 停止并删除容器（保留数据）
docker-compose -f docker-compose-offline.yml down

# 停止并删除容器和数据卷
docker-compose -f docker-compose-offline.yml down -v
```

### 进入容器调试

```bash
# 进入后端容器
docker exec -it library-backend bash

# 进入前端容器
docker exec -it library-frontend sh
```

### 查看容器资源占用

```bash
docker stats
```

---

## 🆙 更新部署

当代码有更新时：

### 本地操作

1. 重新构建镜像

```powershell
.\build-images.bat
```

2. 上传新的 tar 文件到服务器

### 服务器操作

1. 停止服务

```bash
docker-compose -f docker-compose-offline.yml down
```

2. 导入新镜像

```bash
docker load -i library-backend-v1.0.tar
docker load -i library-frontend-v1.0.tar
```

3. 启动服务

```bash
docker-compose -f docker-compose-offline.yml up -d
```

---

## ❗ 常见问题

### Q1: 后端无法连接 MySQL

**现象：** 后端容器启动失败，日志显示数据库连接错误

**解决方案：**

1. 检查 MySQL 是否运行

```bash
sudo systemctl status mysql
```

2. 检查 MySQL 是否监听正确端口

```bash
sudo netstat -tlnp | grep 3306
```

3. 检查 MySQL 用户权限

```sql
SHOW GRANTS FOR 'library_user'@'%';
```

4. 修改 MySQL 绑定地址（如果 MySQL 只监听 127.0.0.1）

```bash
sudo nano /etc/mysql/mysql.conf.d/mysqld.cnf

# 修改或注释
# bind-address = 0.0.0.0

sudo systemctl restart mysql
```

### Q2: 前端访问 404

**现象：** 访问前端显示 404 Not Found

**解决方案：**

1. 检查前端容器状态

```bash
docker logs library-frontend
```

2. 检查 Nginx 配置

```bash
docker exec -it library-frontend cat /etc/nginx/conf.d/default.conf
```

3. 检查前端文件是否存在

```bash
docker exec -it library-frontend ls -la /usr/share/nginx/html
```

### Q3: 端口被占用

**现象：** 启动失败，提示端口已被占用

**解决方案：**

1. 查看端口占用

```bash
sudo netstat -tlnp | grep :80
sudo netstat -tlnp | grep :8080
```

2. 停止占用端口的进程，或修改 `docker-compose-offline.yml` 使用其他端口

```yaml
ports:
  - "8000:80"  # 使用 8000 端口代替 80
```

### Q4: 镜像导入失败

**现象：** `docker load` 失败

**解决方案：**

1. 检查 tar 文件完整性

```bash
tar -tzf library-backend-v1.0.tar > /dev/null
```

2. 检查磁盘空间

```bash
df -h
```

3. 重新下载或传输 tar 文件

### Q5: 服务启动慢

**现象：** 容器启动后需要很长时间才能访问

**解决方案：**

1. 这是正常现象，后端 Spring Boot 应用启动需要 30-60 秒

2. 查看启动日志

```bash
docker-compose -f docker-compose-offline.yml logs -f backend
```

3. 等待看到类似以下日志：

```
Started LibraryManagementApplication in 45.123 seconds
```

---

## 📊 系统要求

### 服务器配置建议

| 组件 | 最低配置 | 推荐配置 |
|------|----------|----------|
| CPU | 2核 | 4核+ |
| 内存 | 4GB | 8GB+ |
| 磁盘 | 20GB | 50GB+ |
| MySQL | 5.7+ | 8.0+ |
| Docker | 20.10+ | 最新稳定版 |

### 端口使用

| 端口 | 服务 | 说明 |
|------|------|------|
| 80 | 前端 | Nginx |
| 8080 | 后端 | Spring Boot API |
| 3306 | MySQL | 数据库（宿主机） |

---

## 📝 安全建议

1. **修改默认密码**
   - 修改 MySQL 密码
   - 修改 JWT_SECRET
   - 修改默认管理员密码

2. **配置防火墙**
   - 只开放必要端口
   - 限制 MySQL 只允许本地连接

3. **定期备份**
   - 备份数据库
   - 备份上传文件目录

```bash
# 数据库备份
mysqldump -u library_user -p library_management > backup_$(date +%Y%m%d).sql

# 文件备份
tar -czf uploads_backup_$(date +%Y%m%d).tar.gz ./uploads
```

4. **日志监控**
   - 定期查看应用日志
   - 监控异常访问

---

## 📞 技术支持

如遇到问题，请提供以下信息：

1. 系统版本：`uname -a`
2. Docker 版本：`docker --version`
3. 容器状态：`docker-compose ps`
4. 容器日志：`docker-compose logs`
5. 错误截图或详细错误信息

---

**部署完成！** 🎉


