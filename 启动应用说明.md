# 🚀 启动应用说明

## ⚠️ 重要提示

如果你使用的是 **PowerShell**，执行 `.bat` 或 `.cmd` 文件需要特殊语法。

---

## 方法一：使用 CMD（推荐）

### 启动后端

**步骤1**：打开新的命令提示符（CMD）
- 按 `Win + R`
- 输入 `cmd`
- 回车

**步骤2**：进入后端目录并启动
```cmd
cd D:\LibraryManagement\library-management-backend
mvnw.cmd spring-boot:run
```

或使用批处理文件：
```cmd
cd D:\LibraryManagement\library-management-backend
run.bat
```

### 启动前端

**步骤1**：打开另一个新的CMD窗口

**步骤2**：进入前端目录并启动
```cmd
cd D:\LibraryManagement\library-management-frontend
npm run dev
```

---

## 方法二：在 PowerShell 中启动

### 启动后端

如果你必须使用PowerShell，需要加上 `cmd /c`：

```powershell
cd D:\LibraryManagement\library-management-backend
cmd /c "mvnw.cmd spring-boot:run"
```

### 启动前端

```powershell
cd D:\LibraryManagement\library-management-frontend
npm run dev
```

---

## 方法三：在 IDE 中启动（推荐开发时使用）

### IntelliJ IDEA / VS Code

1. 打开项目：`D:\LibraryManagement\library-management-backend`
2. 找到主类：`LibraryManagementApplication.java`
3. 右键点击 → Run 'LibraryManagementApplication'

---

## 🔍 如何确认启动成功

### 后端启动成功的标志

看到类似输出：
```
Started LibraryManagementApplication in 12.345 seconds (process running for 13.456)
```

然后访问：
- http://localhost:8080/api/doc.html （API文档）

### 前端启动成功的标志

看到类似输出：
```
  VITE v7.x.x  ready in XXX ms

  ➜  Local:   http://localhost:5173/
  ➜  Network: use --host to expose
```

然后访问：
- http://localhost:5173 （前端页面）

---

## 🔐 测试登录

- **用户名**：`admin`
- **密码**：`admin123`

---

## ❌ 常见问题

### 问题1：端口被占用

**错误信息**：`Port 8080 is already in use`

**解决方法**：
```cmd
netstat -ano | findstr :8080
taskkill /PID <进程ID> /F
```

### 问题2：找不到mvnw.cmd

**解决方法**：确保在正确的目录
```cmd
cd D:\LibraryManagement\library-management-backend
dir mvnw.cmd
```

### 问题3：MySQL连接失败

**解决方法**：
1. 确认MySQL服务运行：`net start mysql`
2. 检查密码是否正确（application-mysql.yml）
3. 确认数据库已创建：登录MySQL查看 `library_management` 数据库

---

## 📝 完整启动流程

### 一键启动（推荐）

**1. 打开第一个CMD窗口** - 启动后端
```cmd
cd D:\LibraryManagement\library-management-backend
mvnw.cmd spring-boot:run
```

**2. 打开第二个CMD窗口** - 启动前端
```cmd
cd D:\LibraryManagement\library-management-frontend
npm run dev
```

**3. 打开浏览器**
- 访问：http://localhost:5173
- 登录：admin / admin123

---

## 🎉 完成！

现在你可以开始使用这个系统了！

