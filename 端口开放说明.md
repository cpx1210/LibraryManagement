# 🔌 端口开放说明

## 📋 需要向服务器管理员申请的端口

### ✅ 必须开放的端口

| 端口 | 服务 | 用途 | 说明 |
|------|------|------|------|
| **80** | 前端（HTTP） | 用户访问入口 | 所有用户通过此端口访问系统 |
| **8080** | 后端API | 后端服务接口 | 前端通过此端口调用后端API |

### ⚠️ 推荐开放的端口

| 端口 | 服务 | 用途 | 说明 |
|------|------|------|------|
| **443** | 前端（HTTPS） | 安全访问入口 | 如果使用SSL证书，用户通过此端口安全访问系统 |

### ❌ 不需要开放的端口

| 端口 | 服务 | 说明 |
|------|------|------|
| **3306** | MySQL数据库 | 数据库只在Docker容器网络内部通信，不需要对外暴露 |

## 🎯 端口开放申请模板

### 申请内容示例

```
标题：图书馆管理系统端口开放申请

需要开放的端口：
1. 80端口（TCP）- HTTP访问
2. 8080端口（TCP）- 后端API服务
3. 443端口（TCP）- HTTPS访问（可选，如果使用SSL证书）

开放范围：
- 80端口：对所有IP开放（公网访问）
- 8080端口：对所有IP开放（公网访问）
- 443端口：对所有IP开放（公网访问，如果使用）

用途说明：
- 80端口：用户通过浏览器访问系统前端界面
- 8080端口：前端应用调用后端API接口
- 443端口：提供HTTPS加密访问（如果配置SSL证书）

服务器IP：[填写你的服务器IP地址]
```

## 🔒 安全建议

### 方案一：标准部署（简单但安全性较低）

- ✅ 开放80端口（前端）
- ✅ 开放8080端口（后端API）
- ❌ 不开放3306端口（数据库）

**适用场景**：内部系统或测试环境

### 方案二：使用Nginx反向代理（推荐，更安全）

- ✅ 开放80端口（Nginx）
- ✅ 开放443端口（Nginx HTTPS）
- ❌ 不开放8080端口（后端通过Nginx代理，不直接暴露）
- ❌ 不开放3306端口（数据库）

**优势**：
- 后端API不直接暴露，更安全
- 可以统一管理SSL证书
- 可以配置负载均衡和限流

**配置方法**：
1. 在服务器上安装Nginx
2. 配置Nginx反向代理，将 `/api` 请求转发到 `localhost:8080`
3. 配置SSL证书（可选）

## 📝 验证端口是否开放

部署完成后，可以使用以下方法验证端口是否正常开放：

### 1. 检查端口监听状态

```bash
# Linux
netstat -tlnp | grep -E ':(80|8080|443)'
# 或
ss -tlnp | grep -E ':(80|8080|443)'

# Windows
netstat -ano | findstr "80 8080 443"
```

### 2. 从外部测试端口

```bash
# 使用curl测试
curl -I http://你的服务器IP:80
curl -I http://你的服务器IP:8080/api/actuator/health

# 使用telnet测试
telnet 你的服务器IP 80
telnet 你的服务器IP 8080
```

### 3. 使用在线工具

- 访问 https://www.yougetsignal.com/tools/open-ports/
- 输入服务器IP和端口号进行测试

## 🚨 常见问题

### Q1: 端口已开放但无法访问

**可能原因**：
1. Docker容器未正常启动
2. 防火墙规则配置错误
3. 服务监听地址配置错误（应该监听0.0.0.0而不是127.0.0.1）

**解决方法**：
```bash
# 检查容器状态
docker compose ps

# 检查容器日志
docker compose logs backend
docker compose logs frontend

# 检查端口监听
docker compose exec backend netstat -tlnp
```

### Q2: 8080端口被其他服务占用

**解决方法**：
1. 修改 `docker-compose.yml` 中的端口映射
2. 例如改为 `"8081:8080"`，则后端API通过8081端口访问

### Q3: 是否需要开放3306端口

**答案**：**不需要**

MySQL数据库只在Docker容器网络内部通信，不需要对外暴露。如果确实需要外部访问数据库（例如使用数据库管理工具），可以：

1. 临时开放（不推荐）：修改 `docker-compose.yml`，将 `ports: []` 改为 `ports: ["3306:3306"]`
2. 使用SSH隧道（推荐）：通过SSH端口转发访问数据库
3. 使用数据库管理工具的内网穿透功能

---

**提示**：如果是在云服务器（如阿里云、腾讯云）上部署，除了在服务器防火墙中开放端口，还需要在云服务商的安全组中配置端口开放规则。

