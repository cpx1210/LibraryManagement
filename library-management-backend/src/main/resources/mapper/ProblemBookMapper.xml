<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.library.management.module.problembook.mapper.ProblemBookMapper">

    <!-- 检测问题书目（ISBN 精确匹配 + 书名相似度匹配） -->
    <select id="detectProblemBook" resultType="com.library.management.module.problembook.entity.ProblemBook">
        SELECT
            book_id,
            book_name,
            author,
            isbn,
            publisher,
            publish_year,
            problem_type,
            source,
            created_by,
            create_time,
            updated_by,
            update_time
        FROM problem_books
        WHERE
            -- ISBN 精确匹配（优先级最高）
            isbn = #{isbn}
            OR
            -- 书名模糊匹配
            (#{bookName} IS NOT NULL AND book_name LIKE CONCAT('%', #{bookName}, '%'))
        ORDER BY
            -- 优先返回 ISBN 匹配的结果
            CASE WHEN isbn = #{isbn} THEN 0 ELSE 1 END,
            -- 然后按书名匹配度排序
            CASE 
                WHEN book_name = #{bookName} THEN 0
                WHEN book_name LIKE CONCAT(#{bookName}, '%') THEN 1
                WHEN book_name LIKE CONCAT('%', #{bookName}, '%') THEN 2
                ELSE 3
            END,
            LENGTH(book_name)
        LIMIT 1
    </select>

</mapper>
