# 新模板适配说明

## 📋 更新概要

本次更新适配了新的检测模板结构，主要包括：

1. **input_data.xlsx** - 书单检测模板更新
2. **keywords.xlsx** - 敏感词模板更新

更新日期：2025-10-30

---

## 📊 新模板结构

### 1. input_data.xlsx（书单检测模板）

**列结构（13列）：**

| 列序号 | 列名 | 说明 | 必填 |
|--------|------|------|------|
| 0 | 书号 | 图书编号 | 否 |
| 1 | 题名 | 书名（主标题） | **是** |
| 2 | 副题名 | 副标题 | 否 |
| 3 | 著者1 | 第一作者 | 否 |
| 4 | 著者2 | 第二作者 | 否 |
| 5 | ISBN | 国际标准书号 | 否 |
| 6 | 出版地 | 出版城市 | 否 |
| 7 | 出版社 | 出版社名称 | 否 |
| 8 | 出版日期 | 出版年份或日期 | 否 |
| 9 | 读者对象 | 目标读者群体 | 否 |
| 10 | 内容简介 | 图书简介 | 否 |
| 11 | 分类号 | 图书分类编号 | 否 |
| 12 | 作品语种 | 原作品语言 | 否 |

**与旧模板的主要差异：**
- ✅ 增加了更多详细信息字段
- ✅ 作者分为著者1和著者2
- ✅ ISBN从第0列移至第5列
- ✅ 书名改为"题名"

### 2. keywords.xlsx（敏感词模板）

**列结构（3列）：**

| 列序号 | 列名 | 说明 | 可选值 |
|--------|------|------|--------|
| 0 | 类型 | 检测类型 | 关键词 / 书名 / 作者 |
| 1 | 关键词 | 敏感词内容 | - |
| 2 | 警报信息 | 命中时的提示信息 | - |

**检测类型说明：**

| 类型 | 检测范围 | 说明 |
|------|---------|------|
| **关键词** | 全局检测 | 在书名、作者、内容简介等所有字段中检测 |
| **书名** | 仅书名 | 只在题名字段中检测 |
| **作者** | 仅作者 | 只在著者1、著者2字段中检测 |

---

## 🔧 代码修改内容

### 1. 实体类更新

#### BookItemDTO.java
- ✅ 新增字段：`bookNumber`, `subtitle`, `author1`, `author2`, `publishLocation`, `publishDate`, `targetAudience`, `contentSummary`, `classificationNumber`, `language`
- ✅ 添加兼容方法：`getAuthor()` 合并著者1和著者2
- ✅ 添加兼容方法：`getPublishYear()` 从出版日期提取年份

#### SensitiveWords.java
- ✅ 新增字段：`detectionType` (检测类型)
- ✅ 新增字段：`alertMessage` (警报信息)

### 2. 解析逻辑更新

#### BooklistCheckServiceImpl.java
- ✅ `parseExcel()` 方法已更新以解析13列新模板
- ✅ `createTemplate()` 方法已更新以生成新模板
- ✅ `isEmptyRow()` 方法已更新以检查13列

### 3. 检测逻辑更新

#### DetectionEngineImpl.java
- ✅ `detectSensitiveWords()` 方法支持按类型检测
  - 关键词类型：全局检测（书名、作者）
  - 书名类型：仅检测题名字段
  - 作者类型：仅检测著者字段

#### SensitiveWordMapper.java
- ✅ 新增方法：`detectSensitiveWordsByType()` 按类型查询敏感词

### 4. 数据库更新

#### update_for_new_template.sql
- ✅ 为 `sensitive_words` 表添加 `detection_type` 字段
- ✅ 为 `sensitive_words` 表添加 `alert_message` 字段
- ✅ 为 `booklist_check_detail` 表添加新的书目字段
- ✅ 创建必要的索引以提升查询性能

---

## 🚀 部署步骤

### 1. 数据库更新

```bash
# 连接到 PostgreSQL 数据库
psql -U your_username -d library_management

# 执行更新脚本
\i library-management-backend/src/main/resources/db/update_for_new_template.sql
```

或使用 SQL 客户端执行 `update_for_new_template.sql` 脚本。

### 2. 后端重新编译

```bash
cd library-management-backend
./mvnw clean package
./mvnw spring-boot:run
```

### 3. 验证更新

1. **下载新模板**
   - 访问系统，下载检测模板
   - 确认模板包含13列新字段

2. **测试敏感词检测**
   - 准备测试数据
   - 上传包含不同类型敏感词的书单
   - 验证检测结果是否正确

---

## 📝 使用示例

### 示例1：书单检测模板填写

```
书号     | 题名                 | 副题名     | 著者1        | 著者2 | ISBN         | ...
BK001    | 深入理解计算机系统   | 原书第3版  | Randal Bryant |       | 9787111681526 | ...
BK002    | Python编程          | 第2版      | Eric Matthes  |       | 9787115545312 | ...
```

### 示例2：敏感词配置

```
类型     | 关键词    | 警报信息
关键词   | 暴力      | 含有暴力相关内容
书名     | 禁书名    | 此书名已被标记为问题书目
作者     | 某作家    | 该作者作品需特别审查
```

---

## ⚠️ 注意事项

1. **向后兼容**
   - 系统保留了 `getAuthor()` 和 `getPublishYear()` 方法以保持向后兼容
   - 旧的检测逻辑仍然可用

2. **数据迁移**
   - 数据库更新脚本会自动处理已有数据
   - 建议在执行前备份数据库

3. **敏感词类型**
   - 默认类型为"关键词"（全局检测）
   - 建议根据实际需求合理配置检测类型

4. **性能优化**
   - 已为 `detection_type` 字段添加索引
   - 大量数据检测时建议分批处理

---

## 🔍 测试建议

### 功能测试清单

- [ ] 上传新格式的 input_data.xlsx，验证解析成功
- [ ] 下载模板，验证模板格式正确
- [ ] 配置"关键词"类型敏感词，验证全局检测
- [ ] 配置"书名"类型敏感词，验证仅书名检测
- [ ] 配置"作者"类型敏感词，验证仅作者检测
- [ ] 导出检测结果，验证结果完整性
- [ ] 验证警报信息是否正确显示

### 性能测试

- [ ] 测试大量书目（1000+）的检测性能
- [ ] 测试大量敏感词（500+）的检测性能
- [ ] 验证数据库查询性能

---

## 📞 问题反馈

如果在使用过程中遇到问题，请：

1. 检查数据库是否正确执行了更新脚本
2. 确认 input_data.xlsx 和 keywords.xlsx 格式正确
3. 查看后端日志获取详细错误信息
4. 提交 Issue 或联系开发人员

---

## 📚 相关文件

- `BookItemDTO.java` - 书目数据传输对象
- `SensitiveWords.java` - 敏感词实体类
- `DetectionEngineImpl.java` - 检测引擎实现
- `BooklistCheckServiceImpl.java` - 书单检测服务
- `update_for_new_template.sql` - 数据库更新脚本

---

**更新完成！** 🎉

现在系统已完全支持新的检测模板结构，可以进行更精确的敏感词检测。
